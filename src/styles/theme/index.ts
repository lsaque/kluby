import breakpoints from './lib/breakpoints'
import mixins from './lib/mixins'
import palette from './lib/palettes'
import typography from './lib/typographies'

export const THEME = Object.freeze({
  breakpoints,
  palette,
  typography,
  mixins,
})
